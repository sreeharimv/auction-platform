# Palace Premier League Auction Platform - Environment Variables
# Copy this file to .env and update with your actual values

# Flask Secret Key
# Generate a random secret key for session management
# You can generate one using: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here-change-this-to-a-random-string

# Admin Password Hash
# This should be a bcrypt hash of your admin password
# To generate a hash, run: python3 -c "import bcrypt; print(bcrypt.hashpw(b'your_password', bcrypt.gensalt(rounds=12)).decode())"
# Default password is "admin123" - CHANGE THIS IN PRODUCTION!
ADMIN_PASSWORD_HASH=$2b$12$dVlj4hIxyz0k3stpOA6fluDdVznyqsOtGKqfVppASDMXKQ92LTkge

# Security Notes:
# - Never commit the .env file to version control
# - Use strong, unique passwords in production
# - Rotate the secret key if it's ever compromised
# - The default password hash above is for "admin123" - change it immediately!

# How to set up:
# 1. Copy this file: cp .env.example .env
# 2. Generate a new secret key (see command above)
# 3. Generate a new password hash (see command above)
# 4. Update the values in .env
# 5. Restart the application
